import React from "react";
import Button from "react-bootstrap/Button";

const AppContainer = () => {
  return (
    <div>
      <Button>Hello</Button>
    </div>
  );
};

export default AppContainer;
